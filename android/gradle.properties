org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
# Kotlin's incremental compiler cache can't compute a relative path between
# the project (D:) and the pub-cache (C:) on Windows when they're on
# different drives - crashes with "this and base files have different
# roots". Incremental compilation isn't required for correctness, just speed.
kotlin.incremental=false
